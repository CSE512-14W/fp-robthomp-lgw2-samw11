(function() {

    var height = 720,
		width = 900;
		margin = 20;

	// some color variables
	var color;

	var svg = d3.select("body").append("svg")
		   	.attr("width", width)
	    	.attr("height", height)
	  		.append("g");

	// TODO layer 3
})();